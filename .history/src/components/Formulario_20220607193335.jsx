import { FormControl, InputLabel, Select, MenuItem, Button, Box} from '@mui/material';

const Formulario = () => {
  return (
    <form>
      <FormControl>
        <InputLabel>Buscar Categoría</InputLabel>
      </FormControl>
    </form>
  )
}

export default Formulario
